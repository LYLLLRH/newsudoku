Todo
1. Sudoku 题库，包括难度；
2. 结局Master Level的局面；


### 行清重
### 列清重
### 块清重
### 行站位
在一行中，通过清重的过程后，如果有个数字只存在其中的一行中，这个数字所在的格子的解就是这个数字
### 列占位
### 块占位
### 对消除（行）
在一行中，如果有2个格子中的可能的数字只有2个，并且这2个数字是相同的。那说明这2个数字只能存在在这2个格子中，所以可以在同一行中，把拥有这2个数字的格子的可能性消除
### 对消除（列）
### 对消除（块）
### 3对消除（行）
在一行中，如果有3个格子中的可能的数字只有3个，并且这3个数字是相同的。那说明这3个数字只能存在在这3个格子中，所以可以在同一行中，把拥有这3个数字的格子的可能性消除
### 3对消除（列）
### 3对消除（块）

### 多重行占位
在一行中有个数字解只存在其中一个块中，那说明这个数字只能存在这个列中，在这个块中的其他位置，这个数字不是解。





		
